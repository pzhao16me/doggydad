---
interface Props {
  title: string;
  description?: string;
  article?: {
    publishedTime?: string;
    tags?: string[];
  };
}

const { title, description = '分享技术见解、编程经验和个人思考的博客', article } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const siteUrl = 'https://pzhao16me.github.io/doggydad';
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- SEO Meta Tags -->
  <title>{title}</title>
  <meta name="description" content={description}>
  <meta name="keywords" content="博客,技术,编程,图灵奖,读书笔记">
  <meta name="author" content="DoggyDad">
  
  <!-- Open Graph -->
  <meta property="og:type" content={article ? 'article' : 'website'}>
  <meta property="og:title" content={title}>
  <meta property="og:description" content={description}>
  <meta property="og:url" content={canonicalURL}>
  <meta property="og:site_name" content="DoggyDad Blog">
  <meta property="og:locale" content="zh_CN">
  
  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary">
  <meta name="twitter:site" content="@Hjfjfjsj">
  <meta name="twitter:title" content={title}>
  <meta name="twitter:description" content={description}>
  
  <!-- Canonical URL -->
  <link rel="canonical" href={canonicalURL}>
  
  <!-- Robots -->
  <meta name="robots" content="index, follow">
  
  <!-- Article specific meta -->
  {article && article.publishedTime && (
    <meta property="article:published_time" content={article.publishedTime}>
  )}
  
  <!-- Google Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-G63268TXPH"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-G63268TXPH');
  </script>
  
  <!-- Structured Data -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": article ? "BlogPosting" : "Blog",
    "name": title,
    "description": description,
    "url": canonicalURL.toString(),
    "author": {
      "@type": "Person",
      "name": "DoggyDad",
      "url": "https://linkedin.com/in/peng-zhao-mei"
    },
    ...(article && {
      "datePublished": article.publishedTime,
      "keywords": article.tags?.join(', ')
    })
  })} />
  
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700;800&display=swap" rel="stylesheet">
</head>
<body>
  <div id="app">
    <header class="header">
      <div class="container header-content">
        <a href="/doggydad/" class="logo">🐕 DoggyDad</a>
        <nav class="nav">
          <a href="/doggydad/">首页</a>
          <a href="/doggydad/about/">关于</a>
          <button class="theme-toggle" id="theme-toggle" aria-label="切换主题">🌙</button>
        </nav>
      </div>
    </header>
    
    <main class="container">
      <slot />
    </main>
    
    <footer class="footer">
      <div class="container">
        <div class="footer-social">
          <a href="https://linkedin.com/in/peng-zhao-mei" target="_blank" rel="noopener">💼 LinkedIn</a>
          <a href="https://x.com/Hjfjfjsj" target="_blank" rel="noopener">𝕏 Twitter</a>
          <a href="https://github.com/pzhao16me" target="_blank" rel="noopener">🐙 GitHub</a>
        </div>
        <p>&copy; 2024 DoggyDad Blog. 使用 Astro 和 ❤️ 构建</p>
      </div>
    </footer>
  </div>
  
  <script>
    // Theme toggle
    const themeToggle = document.getElementById('theme-toggle');
    const html = document.documentElement;
    const savedTheme = localStorage.getItem('theme') || 'light';
    html.setAttribute('data-theme', savedTheme);
    themeToggle.textContent = savedTheme === 'light' ? '🌙' : '☀️';
    
    themeToggle.addEventListener('click', () => {
      const current = html.getAttribute('data-theme');
      const next = current === 'light' ? 'dark' : 'light';
      html.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);
      themeToggle.textContent = next === 'light' ? '🌙' : '☀️';
    });
  </script>
  
  <style is:global>
    :root {
      --color-primary: hsl(210, 100%, 60%);
      --color-primary-dark: hsl(210, 100%, 50%);
      --color-accent: hsl(280, 80%, 65%);
      --color-bg: hsl(0, 0%, 98%);
      --color-surface: hsl(0, 0%, 100%);
      --color-surface-alt: hsl(210, 20%, 96%);
      --color-text: hsl(220, 20%, 15%);
      --color-text-muted: hsl(220, 10%, 45%);
      --color-border: hsl(220, 15%, 90%);
      --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      --radius-md: 0.5rem;
      --radius-lg: 0.75rem;
      --radius-xl: 1rem;
      --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    }
    
    [data-theme="dark"] {
      --color-bg: hsl(220, 20%, 10%);
      --color-surface: hsl(220, 18%, 14%);
      --color-surface-alt: hsl(220, 18%, 18%);
      --color-text: hsl(220, 20%, 95%);
      --color-text-muted: hsl(220, 15%, 65%);
      --color-border: hsl(220, 15%, 25%);
    }
    
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    html { scroll-behavior: smooth; }
    
    body {
      font-family: var(--font-sans);
      line-height: 1.6;
      color: var(--color-text);
      background: var(--color-bg);
      transition: background 0.25s, color 0.25s;
    }
    
    #app { min-height: 100vh; display: flex; flex-direction: column; }
    
    .container { max-width: 1200px; margin: 0 auto; padding: 0 1.5rem; width: 100%; }
    
    .header {
      background: var(--color-surface);
      border-bottom: 1px solid var(--color-border);
      padding: 1.5rem 0;
      position: sticky;
      top: 0;
      z-index: 100;
      backdrop-filter: blur(10px);
    }
    
    .header-content { display: flex; justify-content: space-between; align-items: center; }
    
    .logo {
      font-size: 1.25rem;
      font-weight: 800;
      background: linear-gradient(135deg, var(--color-primary), var(--color-accent));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-decoration: none;
    }
    
    .nav { display: flex; gap: 1.5rem; align-items: center; }
    .nav a { color: var(--color-text); text-decoration: none; font-weight: 500; }
    .nav a:hover { color: var(--color-primary); }
    
    .theme-toggle {
      background: var(--color-surface-alt);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-md);
      padding: 0.5rem 1rem;
      cursor: pointer;
      font-size: 1.125rem;
    }
    
    main { flex: 1; padding: 4rem 0; }
    
    .footer {
      background: var(--color-surface);
      border-top: 1px solid var(--color-border);
      padding: 3rem 0;
      text-align: center;
      color: var(--color-text-muted);
    }
    
    .footer-social { display: flex; justify-content: center; gap: 2rem; margin-bottom: 1.5rem; }
    .footer-social a { color: var(--color-text-muted); text-decoration: none; }
    .footer-social a:hover { color: var(--color-primary); }
    
    a { color: var(--color-primary); }
    
    h1, h2, h3, h4 { font-weight: 700; line-height: 1.2; margin-bottom: 1rem; }
    h1 { font-size: 2.5rem; }
    h2 { font-size: 2rem; }
    h3 { font-size: 1.5rem; }
    
    p { margin-bottom: 1rem; }
    
    code {
      font-family: 'Fira Code', monospace;
      background: var(--color-surface-alt);
      padding: 0.15em 0.4em;
      border-radius: 0.375rem;
      font-size: 0.9em;
    }
    
    pre {
      background: var(--color-surface-alt);
      padding: 1.5rem;
      border-radius: var(--radius-md);
      overflow-x: auto;
      margin-bottom: 1rem;
      border: 1px solid var(--color-border);
    }
    
    pre code { background: none; padding: 0; }
    
    ul, ol { margin-left: 2rem; margin-bottom: 1rem; }
    li { margin-bottom: 0.5rem; }
    
    blockquote {
      border-left: 4px solid var(--color-primary);
      padding-left: 1.5rem;
      margin: 2rem 0;
      font-style: italic;
      color: var(--color-text-muted);
    }
    
    hr { border: none; border-top: 1px solid var(--color-border); margin: 2rem 0; }
    
    table { width: 100%; border-collapse: collapse; margin-bottom: 1rem; }
    th, td { border: 1px solid var(--color-border); padding: 0.75rem; text-align: left; }
    th { background: var(--color-surface-alt); }
  </style>
</body>
</html>
